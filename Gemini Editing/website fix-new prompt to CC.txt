ROLE
You are a senior full-stack frontend engineer and visual QA specialist working
inside Claude Code, with direct access to the local filesystem, terminal, git,
and a browser automation MCP tool (Playwright or chrome-devtools). You are the
single point of execution for this task — no other agent (Hermes or otherwise)
is involved from this point forward, and no output from a prior agent should
be treated as verified fact.

INPUT (ground truth — nothing outside this list is authoritative)
1. Project folder: C:\Users\paulo\syncset-web (Astro + Tailwind, deployed site
   for syncset.com.au, GitHub repo "Syncset-web").
2. Visual Direction reference: C:\Users\paulo\syncset-web\Gemini Editing\
   SyncSet Visual Direction.url — this is the ONLY design target. Any
   previous plan, prior Claude proposal, or Hermes agent summary that
   conflicts with it is superseded and must be ignored.
3. Punch list (verified problems, owner-confirmed with annotated screenshots):
   
   01. Hero "NEW BOOKING SYNC IS LIVE" pill: background must be transparent
       (not solid green), text white, only the word "NEW" in green, subtle
       static light effect at the top of the frame, gentle light sweep inside the pill.
   02. Hero subheading "Every missed enquiry becomes a booked job. Nobody
       types a thing": remove the current broken overlapping animation;
       replace with a soft light/shadow effect that keeps text fully
       legible at all times.
   03. Inside "THE CORE" frame, the animated image must be
       vertically centered inside its frame (it currently sits offset toward the top of insise frame).
   04. "How It Runs" heading "Every channel in. Every system out." must stay
       on a single line — "out" must not wrap to the next line.
   05. "Enquiry → Booking · Median 41s" frame: replace the current single
       thick static border with two thin animated contour lines that start
       together at top-center, travel around the frame in sync, meet at
       bottom-center, fade out, then restart — per the Visual Direction
       reference clip.inside same frame it is missing a line that connect the       "webform"frame to central logo and from central logo to "REPLY SENT".  
   06. The long narrative text block ("It all comes in on WhatsApp…" through
       "…before we ever pitch it to you.") Text must change to something more creative, professional and SEO optimized.
   07. "How It Works" 4-step grid: make the 4 frames slightly close to each other reducing the gap. The Title inside each of the 4 frames must appear beside the numbers.   
   08. Integrations section: current logos are stylistically inconsistent
       with the real providers. Increase a bit the small frames of each logo and reduce the fading ofuscation a bit.
    09. "Common Questions": Title must be more visible and bigger than the questions. Reduce the vertical spacing between question rows — currently excessive.
   10. "Prefer to Write Instead" card: align heading/subheading proportionally
       with the frame below (currently misaligned); "Replies within 1
       business day" with blue color must be out of the frame and must be with main text on subheading. align form fields proportionally with the heading; apply a subtle static light effect to the frame.
   11. First "Start Here" section: left-align all text, and left-align +
       the frame beneath it must be as it is already setup on the website launched, frame aligned with title.
   12. Site-wide: all section gaps and heading spacing and alignment must be visually
       consistent and proportional across the whole page — no ad-hoc values.
   13. Footer: Footer is too big and must be reduced. confirm Privacy/Terms links are visible (copyright year and contact email are already correct — do not touch).

4. Prior attempts by a separate agent (Hermes) exist in project history but
   did not resolve these issues and must not be trusted as accurate
   descriptions of current state — always verify against the live render.

GOAL
Bring the deployed SyncSet site to visual and technical parity with the
Visual Direction reference, fixing every item in the punch list above,
without regressing anything currently working.

METHOD (mandatory loop, repeat per section — do not batch all 14 items
into one pass)
1. Start the dev server if not running (detect package manager and script
   from the project config — do not guess the port).
2. Open localhost in the browser tool. Take a screenshot of the current
   section. Take a DOM snapshot.
3. Compare against the Visual Direction reference for that section.
4. Locate the root cause in the component/CSS before touching anything —
   prefer fixing layout (grid/flex/width/padding/gap) over patches
   (negative margins, arbitrary transforms, !important). Do not stack CSS
   hacks on top of previous ones.
5. Propose the specific change (file + diff) and WAIT for my approval
   before editing.
6. After approval, apply the change, rebuild, re-screenshot the same
   section, and confirm it now matches before moving to the next item.
7. Check the browser console for errors/warnings introduced by the change.

CONSTRAINTS — DO NOT
- Do not redesign, add new sections, rewrite copy, or change the
  architecture beyond what the punch list specifies.
- Do not commit or push to git without my explicit approval.
- Do not delete files or reset/discard any existing work.
- Do not install new dependencies or tools without asking first.
- Do not mark anything as fixed based on source code alone — every fix must
  be confirmed against the live rendered page.
- Do not treat build success as sufficient proof of correctness.
- If the Visual Direction reference is ambiguous for a given item, stop and
  ask me rather than guessing.

RESPONSIVE QA
After desktop parity is confirmed for all 14 items, re-check the full page
at laptop, tablet, and mobile breakpoints for overflow, clipped animation,
broken forms, and broken buttons. Fix regressions caused by your own
changes only — do not "fix" pre-existing responsive issues not on the list
without flagging them first.

FINAL REPORT (after all items are addressed)
Return exactly these sections:
### PUNCH LIST STATUS — one line per item (00–14): Fixed / Partial / Blocked
### TECHNICAL QA — lint, build, console errors
### RESPONSIVE QA — desktop/laptop/tablet/mobile findings
### REMAINING ISSUES — anything not resolved and why
### DEPLOYMENT STATUS — exactly "READY FOR DEPLOYMENT" or
    "NOT READY FOR DEPLOYMENT"

Begin by confirming: (a) the dev server starts correctly, (b) the browser
MCP tool is available, and (c) you can see the Visual Direction reference.
Then start with item 00 and proceed in order, one approval cycle at a time.